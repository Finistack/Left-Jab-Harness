# ── Secrets & runtime config (NEVER commit) ──
.secrets.env
**/.secrets.env
config.env
**/config.env
*.local
*.local.env

# ── Bot runtime state ──
.pr-bot-state/
**/.pr-bot-state/
*.log

# ── OS / editor noise ──
.DS_Store
*.swp
*~

# ── Python bytecode (github-pr-review.py + its py_compile syntax guard) ──
__pycache__/
*.py[cod]

# ── Worktrees (Claude Code) ──
.claude/worktrees/
